export enum ContainerStatusEnum {
    CREATED = 'created',
    RESTARTING = 'restarting',
    RUNNING = 'running',
    REMOVING = 'removing',
    PAUSED = 'paused',
    EXITED = 'exited',
    DEAD = 'dead',
}
